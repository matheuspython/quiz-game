import styled from 'styled-components';

export const Container = styled.main`
  main{
    width: 100%;
    max-width: 1280px;
    margin:0 auto;
  }
  ul{
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 15px;
  }

`; 
